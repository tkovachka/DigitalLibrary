@model List<Book>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<div class="container">
    <div class="btn-group">
        <a asp-action="Create" class="btn btn-success">Create New</a>
        <a asp-action="Import" class="btn btn-outline-success">Import</a>
    </div>
    <hr />
    
    @for (int i = 0; i < Model.Count; i++)
    {
        var item = Model[i];

        if (i % 4 == 0)
        {
            @:<div class="row mb-4">
        }

        <div class="col-12 col-md-6 col-lg-3 mb-4">
            <div class="card h-100 shadow-sm">
                @if (!string.IsNullOrWhiteSpace(item.ThumbnailUrl))
                {
                    <img src="@item.ThumbnailUrl" class="card-img-top" alt="@item.Title" />
                }

                <div class="card-body d-flex flex-column">
                    <h5 class="card-title">@item.Title</h5>
                    @if (!string.IsNullOrWhiteSpace(item.Subtitle))
                    {
                        <h6 class="card-subtitle mb-2 text-muted">@item.Subtitle</h6>
                    }

                    <p class="card-text mb-2">
                        <strong>ISBN-10:</strong> @item.Isbn10<br />
                        <strong>Page count:</strong> @item.PageCount<br />
                        <strong>Average rating:</strong> @item.AverageRating<br />
                        <strong>Publisher:</strong> @item.Publisher?.Name
                    </p>

                    <p class="fw-semibold mb-1"><strong>Authors</strong></p>
                    <ul class="list-unstyled mb-2">
                        @foreach (var author in item.Authors)
                        {
                            <li>@author.Name</li>
                        }
                    </ul>

                    <p class="fw-semibold mb-1"><strong>Categories</strong></p>
                    <ul class="list-inline mb-0">
                        @foreach (var cat in item.Categories)
                        {
                            <li class="list-inline-item badge bg-dark text-white">@cat.Name</li>
                        }
                    </ul>
                </div>

                <div class="card-footer bg-white border-0">
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </div>
            </div>
        </div>

        if (i % 4 == 3 || i == Model.Count - 1)
        {
            @:</div>
        }
    }
</div>
